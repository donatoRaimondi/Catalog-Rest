import { Routes } from '@angular/router';
import { ProductsPageComponent } from './products/product-page/products-page.component';

export const routes: Routes = [
  { path: '', component: ProductsPageComponent },
];
